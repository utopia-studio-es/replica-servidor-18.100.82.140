# Copiar a /etc/ssh/sshd_config.d/50-replica-18.100.82.140.conf (como root, 0644)
# OpenSSH usa el PRIMER valor: este archivo debe ordenarse antes de 60-cloudimg-settings.conf

PasswordAuthentication yes
KbdInteractiveAuthentication no
